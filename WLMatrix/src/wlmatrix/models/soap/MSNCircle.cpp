#pragma once
#include "MSNCircle.h"

        void MSNCircle::setHostedDomain(std::string hostedDomain){
            _hostedDomain = hostedDomain;
        };

        void MSNCircle::setProfileLastUpdated(std::string profileLastUpdated){
            _profileLastUpdated = profileLastUpdated;
        };

        void MSNCircle::setCreateDate(std::string createDate){
            _createDate = createDate;
        };

        void MSNCircle::setLastUpdated(std::string lastUpdated){
            _lastUpdated = lastUpdated;
        };

        void MSNCircle::setRole(std::string role){
            _role = role;
        };

        void MSNCircle::setProfileLastUpdate(std::string name){
            _name = name;
        };

        void MSNCircle::setIsFavorite(bool isFavorite){
            _isFavorite = isFavorite;
        };

        void MSNCircle::setIsFamily(bool isFamily) {
            _isFamily = isFamily;
        }

        void MSNCircle::setIsDeleted(bool isDeleted) {
            _isDeleted = isDeleted;
        };